namespace AllOverIt.Pagination
{
    /// <summary>Indicates the direction of pagination requests.</summary>
    public enum PaginationDirection
    {
        /// <summary>Move forward when requesting the next page.</summary>
        Forward,

        /// <summary>Move backwards when requesting the next page.</summary>
        Backward,
    };
}
