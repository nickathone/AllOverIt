namespace AllOverIt.Filtering.Filters
{
    /// <summary>Represents a filter operation that determines if a string value is not contained within another string value.</summary>
    public interface INotContains : IStringFilterOperation
    {
    }
}