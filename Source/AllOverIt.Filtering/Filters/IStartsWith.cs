namespace AllOverIt.Filtering.Filters
{
    /// <summary>Represents a filter operation that determines if a string value starts with another string value.</summary>
    public interface IStartsWith : IStringFilterOperation
    {
    }
}