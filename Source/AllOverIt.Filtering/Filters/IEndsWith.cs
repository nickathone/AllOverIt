namespace AllOverIt.Filtering.Filters
{
    /// <summary>Represents a filter operation that determines if a string value ends with another string value.</summary>
    public interface IEndsWith : IStringFilterOperation
    {
    }
}