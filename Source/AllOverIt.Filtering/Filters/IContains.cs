namespace AllOverIt.Filtering.Filters
{
    /// <summary>Represents a filter operation that determines if a string value is contained within another string value.</summary>
    public interface IContains : IStringFilterOperation
    {
    }
}